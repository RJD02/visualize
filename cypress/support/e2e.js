// Cypress support file (minimal)
// Load commands or global configuration for Cypress tests.
// Kept minimal for CI headless runs.
// eslint-disable-next-line no-undef
before(() => {
  cy.log('Support file loaded');
});
