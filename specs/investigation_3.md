You are a senior frontend + systems debugging engineer.

This task CANNOT be completed by static code inspection alone.
You MUST run the real UI and observe actual DOM behavior.

Context:
- The app generates PlantUML → SVG diagrams.
- SVG is rendered inline.
- CSS animation rules are injected.
- Animations are NOT visible in the real UI.
- Previous fixes did not resolve the issue.

Goal:
Empirically identify why SVG animations do not apply, and fix the system until animation is visibly working in the browser.

---

MANDATORY EXECUTION ENVIRONMENT

- You MUST open the actual application UI in a browser inside VSCode.
- Use Playwright / Puppeteer / VSCode browser.
- You MUST inspect the live DOM, not inferred structure.

---

STEP 1 — LOAD AND OBSERVE (NO CODE CHANGES YET)

1. Launch the UI.
2. Navigate to a page that renders an animated SVG.
3. Confirm visually:
   - No animation is visible.
4. Inspect the SVG element in DevTools.

STOP and record:
- Is the SVG inline (<svg>)?
- Does it contain a <style> tag?
- Are animation rules present?

DO NOT FIX ANYTHING YET.

---

STEP 2 — DOM REALITY CHECK (MANDATORY)

In DevTools Console, run:

1. document.querySelectorAll("svg").length
2. document.querySelectorAll("svg g").length
3. document.querySelectorAll("svg style").length

Then for EACH animation selector generated by the system:
- Run:
  document.querySelectorAll("<selector>").length

Log:
- Selector
- Match count

If ANY selector returns 0:
- Mark it as INVALID.

---

STEP 3 — FORCE A VISIBLE CHANGE (CRITICAL)

Manually run in DevTools:

1. document.querySelector("svg g").style.opacity = "0.2"
2. document.querySelector("svg g").style.stroke = "red"
3. document.querySelector("svg g").style.animation = "none"

If these do NOT visually change the diagram:
- The SVG is NOT being modified as expected.
- Identify why (embedding, shadow DOM, iframe, etc.)

---

STEP 4 — INJECT A GUARANTEED ANIMATION

Manually inject:

```js
const style = document.createElement("style");
style.textContent = `
svg g {
  animation: debugblink 1s infinite alternate !important;
}
@keyframes debugblink {
  from { opacity: 1; }
  to { opacity: 0.3; }
}
`;
document.querySelector("svg").appendChild(style);